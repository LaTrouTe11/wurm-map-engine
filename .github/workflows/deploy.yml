- name: Génération
        run: |
          mkdir -p sqlite
          # Vérifiez bien ces lignes ci-dessous :
          lftp -u "${{ secrets.LEGION_USER }},${{ secrets.LEGION_PASS }}" sftp://${{ secrets.LEGION_HOST }}:${{ secrets.LEGION_PORT }} <<EOF
          set sftp:auto-confirm yes
          set ssl:verify-certificate no
          set xfer:clobber on
          # ... reste du script
